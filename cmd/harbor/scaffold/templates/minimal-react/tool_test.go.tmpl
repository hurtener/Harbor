// Generated by `harbor scaffold` from harbor.yaml's
// tools.custom[] entry for {{.Tool.Name | printf "%q"}}.
//
// Re-running `harbor scaffold --patch` will NOT overwrite this file —
// extend the test as you implement Handle.
package tools

import (
	"context"
	"testing"
)

// Test{{.Tool.GoIdent}}_Stub asserts the stub returns its zero-value
// output without error. Once you implement {{.Tool.GoIdent}}, expand
// the test to cover the actual contract.
func Test{{.Tool.GoIdent}}_Stub(t *testing.T) {
	t.Parallel()
	out, err := {{.Tool.GoIdent}}(context.Background(), {{.Tool.GoIdent}}Input{})
	if err != nil {
		t.Fatalf("{{.Tool.GoIdent}}: unexpected error: %v", err)
	}
	_ = out
}
