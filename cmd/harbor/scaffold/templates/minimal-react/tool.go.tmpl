// Generated by `harbor scaffold` from harbor.yaml's
// tools.custom[] entry for {{.Tool.Name | printf "%q"}}.
//
// Re-running `harbor scaffold --patch` will NOT overwrite this file —
// edit Handle freely to implement your tool.
//
// {{.Tool.Description}}
package tools

import "context"

// {{.Tool.GoIdent}}Input is the typed input shape for {{.Tool.Name | printf "%q"}}.
// JSON-schema is derived from this struct via reflection at register
// time; the field names + types come from harbor.yaml.
type {{.Tool.GoIdent}}Input struct {
{{- range .Tool.Input}}
	{{.GoIdent}} {{.GoType}} `json:{{.JSONName | printf "%q"}}`
{{- end}}
}

// {{.Tool.GoIdent}}Output is the typed output shape for {{.Tool.Name | printf "%q"}}.
type {{.Tool.GoIdent}}Output struct {
{{- range .Tool.Output}}
	{{.GoIdent}} {{.GoType}} `json:{{.JSONName | printf "%q"}}`
{{- end}}
}

// {{.Tool.GoIdent}} is the {{.Tool.Name | printf "%q"}} tool's handler.
//
// TODO: replace this stub with your real implementation. The
// scaffolded body returns a zero-valued output so the package compiles
// out of the box.
func {{.Tool.GoIdent}}(ctx context.Context, in {{.Tool.GoIdent}}Input) ({{.Tool.GoIdent}}Output, error) {
	_ = ctx
	_ = in
	return {{.Tool.GoIdent}}Output{}, nil
}
