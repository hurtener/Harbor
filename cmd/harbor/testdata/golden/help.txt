harbor is the command-line entry point for the Harbor agent runtime SDK.

Subcommands fall into three groups:

  Local dev loop      init, dev, console, scaffold, validate, skill
  Production          serve, token, postgres
  Run inspection      inspect-events, inspect-runs, inspect-topology,
                      composition-preview, tui
  Build information   version

Subcommands without a real implementation yet stub-fail with a
structured error pointing to the phase that will implement them. See
RFC-001-Harbor.md §8 for the settled subcommand surface and
docs/plans/README.md for the implementation schedule.

Usage:
  harbor [command]

Available Commands:
  composition-preview preview the effective composition a Runtime would compose for an agent
  console             serve the Harbor Console (static build + Protocol surface)
  dev                 boot the local Runtime + Protocol server
  help                Help about any command
  init                drop a tiered harbor.yaml + AGENTS.md/CLAUDE.md/README.md into a directory
  inspect-events      tail or filter the event bus of a running Runtime
  inspect-runs        list recent runs and inspect a run's trajectory
  inspect-topology    render a run's node graph as ASCII
  postgres            inspect and operate PostgreSQL persistence
  scaffold            generate a new agent skeleton from a template
  serve               serve the headless Runtime + Protocol surface behind a production JWKS verifier
  skill               manage the runtime skill catalog (import / rm)
  token               self-issue JWTs harbor serve verifies (bring-your-own-issuer)
  tui                 attach the native conversation client to a Runtime
  validate            validate config / skills / agent definitions without booting
  version             print harbor semver, protocol version, and build hash

Flags:
  -h, --help    help for harbor
      --json    emit machine-readable JSON output instead of human-readable text
      --quiet   suppress informational output (errors still emit)

Use "harbor [command] --help" for more information about a command.
