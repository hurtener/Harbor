{"base":"4795338cda7a16227e3d17ad24e11943c0ab1356","patch":"diff --git a/docs/notes/portable-context-tracker.md b/docs/notes/portable-context-tracker.md\nindex 2d3eddf..b0dc565 100644\n--- a/docs/notes/portable-context-tracker.md\n+++ b/docs/notes/portable-context-tracker.md\n@@ -64,3 +64,14 @@ runs 128 sessions with competing dispatch/reconciliation through separate pools.\n The phase 269 smoke runs the same scenarios when `HARBOR_PG_DSN` is provided and\n reports an explicit skip otherwise. These tests do not replace the full final-tree\n regression, coverage and preflight gates above.\n+\n+## Request diagnostics publication\n+\n+The bounded diagnostics implementation is published at `1f2a184`. Phase 268\n+smoke passed 10 checks with no skips or failures, and the affected core race\n+suites, scoped Go lint, full Markdown and canonical generators passed.\n+Expanded Protocol tests exposed missing independent compatibility-list entries\n+for `llm.context.prepared` and `sessions.reconcile_context`. Both are now explicit;\n+the exact event-name and single-source method checks remain strict. The complete\n+Protocol documentation-generator and single-source checker race suites pass.\n+This correction does not replace final-tree preflight or the other release gates.\ndiff --git a/internal/protocol/singlesource/singlesource.go b/internal/protocol/singlesource/singlesource.go\nindex 05a6006..2c4d644 100644\n--- a/internal/protocol/singlesource/singlesource.go\n+++ b/internal/protocol/singlesource/singlesource.go\n@@ -231,12 +231,12 @@ var CanonicalMethods = map[string]struct{}{\n \t// Console-Tasks-page cluster — two methods.\n \t\"tasks.list\": {},\n \t\"tasks.get\":  {},\n-\t// Console-Sessions-page cluster — four methods (two reads + the\n-\t// data-lifecycle erasure verb + the rename verb).\n-\t\"sessions.list\":      {},\n-\t\"sessions.inspect\":   {},\n-\t\"sessions.delete\":    {},\n-\t\"sessions.set_title\": {},\n+\t// Session methods: two reads, erasure, reconciliation and rename.\n+\t\"sessions.list\":              {},\n+\t\"sessions.inspect\":           {},\n+\t\"sessions.delete\":            {},\n+\t\"sessions.reconcile_context\": {},\n+\t\"sessions.set_title\":         {},\n \t// Session-turns read pair — the turn-projection surface (routes are\n \t// pinned explicitly; never derived generically).\n \t\"sessions.turns.list\": {},\n","sha256":"0c9c4f7e62d0efb90a6c998b8b66dd895b2bef11a14823fc4a8b3dca570a1738","tree":"c01ff9f246b17518cda5ecc05d039759ac0a038b","message":"fix(protocol): include session reconciliation in single-source checks\n\nAdd the existing sessions.reconcile_context method to the independently maintained canonical-method checker and keep its literal-drift enforcement intact. Together with the explicit prepared-event oracle entry, both previously failing compatibility suites pass under Go 1.26.4 race testing. Update the repository tracker without declaring full release acceptance."}